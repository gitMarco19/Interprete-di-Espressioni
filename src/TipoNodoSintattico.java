/**
 * L'<i>enum</i> {@code TipoNodoSintattico} indica il tipo di {@code Token}
 * che può contenere un <i>nodo</i> dell'<i>albero sintattico</i>.
 * 
 * @author Marco Scanu
 */
public enum TipoNodoSintattico {
	OPERATORE,
	NUMERO,
	VARIABILE;
}
